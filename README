# Kernel-Elemental For All Hi6250QQ5NB7w

## Compatibility
*Android 15 And Androtall /usr/bin/gcc gcc /usr/bin/gcc-4.8 60 --slave /usr/bin/g++ g++ /usr/bin/g++-4.8;

When completed, you must change to the gcc you want to work with by default. Type in your terminal:
sudo update-alternatives --config gcc

To verify if it worked. e "./clean.sh" to clean the previous compilation and repeat again. "./build.sh"... ..enjoy..
   
